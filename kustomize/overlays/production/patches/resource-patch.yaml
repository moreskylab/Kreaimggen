# Strategic Merge Patch – production resource limits
# Targets: backend Deployment
- op: replace
  path: /spec/template/spec/containers/0/resources
  value:
    requests:
      cpu: "500m"
      memory: "512Mi"
    limits:
      cpu: "2000m"
      memory: "1Gi"
